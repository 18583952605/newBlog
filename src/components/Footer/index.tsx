import {DefaultFooter} from '@ant-design/pro-layout'
import defaultSettings from '../../../config/defaultSettings';

export default () => {
  return (
    <DefaultFooter copyright={`2019.6.7 ${defaultSettings.title}`}/>
  )
}
